#include <stdio.h>
//%o=to convert it to octal
//%x=to convert it to hexa.
int main(){
    int x;
    scanf("%d",&x);
    printf("Hexadecimal: %x\n",x);
    printf("Octal: %o",x);
    return 0;

}