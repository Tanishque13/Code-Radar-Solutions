// Your code here...
#include<stdio.h>
#include<string.h>
int main(){
    char arr[100];
    int freq[100];
    fgets(str,sizeof(arr),stdin);
    for(int i=0;i<sizeof(str);i++){
        freq[arr[i]]++;
        max_freq=freq{arr[0]};
        ele=arr[0];
        for(int i=0;i<sizeof(str);i++){
            if(freq[arr[i]]>max_freq){
                max_freq=freq[arr[i]];
                ele=arr[i];
            }
        }
        printf("%c",arr[i]);
    }
    
        
        
        

    }

    
}